import {NextRequest,NextResponse} from "next/server";import {makeSession,sessionCookie} from "@/lib/auth";
export async function POST(req:NextRequest){const {password}=await req.json();if(password!==process.env.ADMIN_PASSWORD)return NextResponse.json({error:"Ongeldig wachtwoord"},{status:401});const r=NextResponse.json({ok:true});r.cookies.set(sessionCookie,makeSession(),{httpOnly:true,secure:process.env.NODE_ENV==="production",sameSite:"lax",path:"/",maxAge:604800});return r;}
